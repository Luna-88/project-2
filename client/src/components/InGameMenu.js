export default function InGameMenu() {
    return (
        <div className="in-menu-container">
            <button
                className="in-menu-button"
                onClick
            >Inventory</button>
            <button
                className="in-menu-button"
                onClick
            >Tooltips</button>
            <button
                className="in-menu-button"
                onClick
            >Options</button>
            <button
                className="in-menu-button"
                onClick
            >Save Game</button>
            <button
                className="in-menu-button"
                onClick
            >Exit Game</button>
        </div>
    )
}